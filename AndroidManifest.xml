<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.waterworks.watermeter"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="9"
        android:targetSdkVersion="18" />

    <application
        android:name="com.waterworks.watermeter.base.BaseApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.waterworks.watermeter.activitys.LoginActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- 主页面 -->
        <activity android:name="com.waterworks.watermeter.activitys.MainActivity" />
        <!-- 抄表 -->
        <activity android:name="com.waterworks.watermeter.activitys.meterreading.MeterreadingActivity" />
        <!-- 维修 -->
        <activity android:name="com.waterworks.watermeter.activitys.maintenance.MaintenanceActivity" />
        <!-- 下载 -->
        <activity android:name="com.waterworks.watermeter.activitys.download.DownloadActivity" />
        <!-- 上传 -->
        <activity android:name="com.waterworks.watermeter.activitys.upload.UploadActivity" />
    </application>

</manifest>